<h1>Articles</h1>
<ul>
	<% @articles.each do |article| %>
		<li>
		<!-- <a href="<%= myarticle_path(article) %>"> -->
		<!-- <a href="<%= myarticle_url(article) %>">  -->
		<!-- <%= article.title%> -->
		<%= link_to article.title, article%>
		</a>
		</li>
	<% end %>
</ul>